# Azure Production Dependencies - Lightweight, API-based services only
# Excludes: PyTorch, transformers, Ollama, ChromaDB (too heavy for Azure Free Tier)

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
pydantic-core==2.14.5  # Pin to stable version compatible with Python 3.12
email-validator==2.2.0

# Database - MongoDB
pymongo==4.6.1
motor==3.3.2

# Task Queue
celery==5.3.4

# AI/ML - API-based providers ONLY (no local models)
anthropic==0.7.7  # Claude API (paid, high quality)
openai==1.3.7  # OpenAI API (paid, embeddings cheap)
groq==0.4.1  # Groq API - FREE tier, open source models (Llama, Mixtral)
pinecone-client==2.2.4  # Pinecone vector DB - FREE tier (100k vectors)

# LangGraph for conversational AI
langgraph==0.2.50  # State-based agent orchestration (includes langchain-core) - updated for Python 3.12 compatibility
langchain-core==0.3.21  # Explicitly pin compatible langchain-core
langsmith>=0.1.147  # Use newer version with Python 3.12 fixes

# HTTP & Web Scraping
httpx==0.25.2
aiohttp==3.9.1
beautifulsoup4==4.12.2
lxml==4.9.3
feedparser==6.0.10
playwright==1.40.0

# Email
python-dotenv==1.0.0
jinja2==3.1.2

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6

# Utilities
pyyaml==6.0.1
pytz==2023.3
dateparser==1.2.0
python-slugify==8.0.1

# Monitoring & Logging
structlog==23.2.0
sentry-sdk[fastapi]==1.38.0

# Production WSGI server
gunicorn==21.2.0
